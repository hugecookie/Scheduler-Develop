spring:
  datasource:
    url: jdbc:mysql://localhost:3306/schedule_develop
    username: root
    password: 1q2w3e4r!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # 또는 create, validate, none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
